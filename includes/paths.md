There are several utilities available to manipulate the paths of files:

-   **realpath** returns the absolute path name of a file
-   **basename** returns only the filename
-   **dirname** returns the file's directory
