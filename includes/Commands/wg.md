**wg** is the main CLI frontend for Wireguard, the UDP-based tunneling protocol and application that was introduced with kernel 5.6.

```sh
# Fedora
dnf install wireguard-tools

# Ubuntu
apt install wireguard
```

