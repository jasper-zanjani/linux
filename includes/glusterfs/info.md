Gluster (1) is a Scale Out Network Attached Storage file system with a client/server model.
A management daemon (glusterd) runs on each server and manages a brick process (glusterfsd) which exports the underlying on-disk storage.
{: .annotate }

1.  

    --8<-- "includes/glusterfs/callout.md"
