GlusterFS has some advantages:

-   Metadata is stored along with files, and there is no single metadata server
-   Files are stored on-disk using local filesystems, not a custom filesystem format
-   Clusters can be set up easily in about 5 commands
