```sh
# See if a website name is available (.com by default)
tldx $NAME

# Specify top-level domain
tldx $NAME -t $TLD

# Specify popular top-level domains (.com, .co, .io, etc)
tldx $NAME --tld-preset popular
```
