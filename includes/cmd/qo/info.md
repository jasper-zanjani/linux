[qo](https://github.com/kiki-ki/go-qo) is a TUI to query JSON and CSV with SQL.
Like vim it is modal, but unlike vim it launches in editing mode to edit the default SQL query. 
++tab++ will switch to a navigation mode using vim keybindings.

