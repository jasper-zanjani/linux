The peer comands are used to manage the trusted server pool (TSP).

```sh title="peer commands"
# View peer status
gluster peer status
```

The volume commands are used to setup and manage Gluster volumes.

```sh title="volume commands"
# View volumes
gluster volume list

# View running bricks
gluster volume status
```
